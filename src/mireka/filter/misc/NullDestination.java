package mireka.filter.misc;

import mireka.filter.Destination;

/**
 * NullDestination specifies that mail sent to the recipient must be dropped.
 */
public class NullDestination implements Destination {
    // The class type itself holds the information
}
